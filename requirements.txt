# Deliberately small: face detection moved into the browser, so no OpenCV,
# no MediaPipe, no PyAV and no WebRTC server stack. Keeps the image inside a
# 512 MB free-tier instance and makes cold starts fast.
streamlit>=1.40,<2
streamlit-mic-recorder>=0.0.8
requests>=2.31
python-dotenv>=1.0.1
plotly>=5.22
